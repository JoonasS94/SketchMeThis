/*using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class PartGivingScore : MonoBehaviour
{

    void OnTriggerEnter2D(Collider2D other)
    {
        Debug.Log("Line is hitting something OnTriggerEnter: " + other);

        if (other.gameObject.tag == "ScoreObjectTag")
        {
            //other.GetComponent<MeshCollider>().enabled = false;
            Debug.Log("Drawing Line hit to a score object!");
        }
    }
}
*/